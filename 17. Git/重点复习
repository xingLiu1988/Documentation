                          VERSION CONTROL常用指令
1. 初始化本地仓库
    >> git init
    
2. 添加文件到临时存贮仓库
    >> git add fileName
    >> git add .
    
3. 提交版本文件
    >> git commit -m “commit message”
    
4. 查看临时仓库
    >> git status
    
5. 查看历史commit日志
    >> git log
    >> git log --pretty-oneline

6. git checkout checkoutFile
    >> 返回上次commit记录点
    >> 一般用于返回之前记录，因为无法还原
    
7. 如何链接远程仓库
    >> git remote add orgin https://github.com/angelabauer/story.git
    >> 链接远程仓库，并命名远程仓库为origin
    >> 该作用是为了将来push本地修改的文件到远程仓库

8. 如何推送本地仓库到远程仓库
    >> git push -u origin master
    >> 推送本地文件到远程名为origin的master仓库

9. 如何删除远程链接
    >> git remote remove origin

10. 创建新的分支
    >> git checkout -b newBranch

11. 跳转分支
    >> git checkout master

12. 删除分支
    >> git branch -d newBranch

13. 查看未来版本，方便回到过去
    >> git reflog

14. 退回之前版本
    >> HEAD表示当前版本
    >> git reset --hard HEAD^
    >> git reset --hard commit_id

第一次运行配置
    >> $ git config --global user.name "John Doe" //设置用户名
    >> $ git config --global user.email johndoe@example.com //设置邮箱
    >> $ git config --list --show-origin //显示git信息
    >> https://git-scm.com/book/zh/v2/%E8%B5%B7%E6%AD%A5-%E5%88%9D%E6%AC%A1%E8%BF%90%E8%A1%8C-Git-%E5%89%8D%E7%9A%84%E9%85%8D%E7%BD%AE //第一次配置网站
