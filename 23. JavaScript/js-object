Array                                   String
    array1.concat(array2)                   string.charAt(index)
    array.copyWithin(target, start, end)    string.charCodeAt(index)
    array.entries()                              string.concat(string1)
    array.every()                                 string.endsWith(searchvalue)
    array.fill()                      String.fromCharCode(65)
    array.filter(function)                    string.includes(searchvalue)
    array.find(function)                      string.indexOf(searchvalue)
    array.findIndex(function)                 string.lastIndexOf(searchvalue)
    array.forEach(function)                   string.localeCompare(compareString)
    Array.from(string)                      string.match(regexp)
    array.includes(searchvalue)                  string.repeat(count)
    array.indexOf(searchvalue)                   string.replace(searchvalue, newvalue)
    isArray()                   search()
    join()                      slice(start, end)
    keys()
    lastIndexOf()
    map()
    pop()
    push()
    reduce()
    reduceRight()
    reverse()
    shift()
    slice()
    some()
    sort()
    splice()
    toString()
    unshift()
    valueOf()
